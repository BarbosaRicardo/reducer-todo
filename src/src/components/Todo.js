import React from 'react'
 const Todo = props => {

    return (
        <div>
            <p
                className={props.item.complete ? "complete" : ""}
                
        </div>
    )
 }